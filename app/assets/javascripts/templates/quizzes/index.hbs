<h2>Quizzes</h2>

<table>
  <tr>
    <th>title</th>
    <th>progress</th>
  </tr>

  {{#each quizzes}}
  <tr class="start-quiz" data-quiz={{id}}>
    <td>{{title}}</td>
    <td>{{progress}}</td>
  </tr> 
  {{/each}}
</table>